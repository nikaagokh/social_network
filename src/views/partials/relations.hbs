<div class="relations-container">
    <div class="relations-header">
        <button class="button button-icon display-bn" id="accBackButton">
            <i class="material-symbols-outlined">chevron_left</i>
        </button>
        <span>ურთიერთობები</span>
        <span class="material-symbols-outlined">beenhere</span>
    </div>
    <div class="relations-content">
        <div class="relations-flex">
            <div class="relations-type-actions">
                <button class="button button-active" id="relationsSendButton">
                    <i class="material-symbols-outlined">unarchive</i>
                    გაგზავნები
                </button>
                <button class="button" id="relationsReceiveButton">
                    <i class="material-symbols-outlined">archive</i>
                    მიღებულები
                </button>
            </div>
            <div class="in-relations-list">
                {{#notEmpty sendRequests}}
                {{#each sendRequests as |request i|}}
                <div class="header__input-list-item list-divider" data-userId="{{this.userId}}">
                    <i class="material-symbols-outlined" style="font-size: 2.5rem;">person</i>
                    <span class="header__input-list-content">
                        <h4 class="header__input-list-title">{{this.name}}</h4>
                        <p class="header__input-list-price">{{this.date}}</p>
                    </span>
                    <button class="button button-icon in-relations-more" data-requestId="{{this.requestId}}">
                       <i class="material-symbols-outlined">more_horiz</i>
                    </button>
                </div>
                {{/each}}
                {{else}}
                თქვენ არავისთან გაგიგზავნიათ მოთხოვნა!
                {{/notEmpty}}
            </div>
            <div class="out-relations-list">
                {{#notEmpty receiveRequests}}
                {{#each receiveRequests as |request i|}}
                <div class="header__input-list-item list-divider" data-userId="{{this.userId}}">
                    <i class="material-symbols-outlined" style="font-size: 2.5rem;">person</i>
                    <span class="header__input-list-content">
                        <h4 class="header__input-list-title">{{this.name}}</h4>
                        <p class="header__input-list-price">{{this.date}}</p>
                    </span>
                    <button class="button button-icon out-relations-more" data-requestId="{{this.requestId}}">
                       <i class="material-symbols-outlined">more_horiz</i>
                    </button>
                </div>
                {{/each}}
                {{else}}
                თქვენთვის არავის გამოუგზავნია მოთხოვნა!
                {{/notEmpty}}
            </div>
        </div>
    </div>
</div>

<script src="/public/js/relations.js" type="module"></script>